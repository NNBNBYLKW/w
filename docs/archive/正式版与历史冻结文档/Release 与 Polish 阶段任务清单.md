# Release 与 Polish 阶段任务清单

## 1. 文档目的

本清单只记录当前版本的 **release-facing / polish-facing** 小项，用于帮助冻结当前 v1 边界后的收口工作。

本清单不是：

- 新功能路线图
- 下一阶段需求池
- frozen v1 的扩 scope 入口

## 2. 当前已收口项

当前 release-facing / polish-facing 已收口的事项包括：

- sidebar 去除旧 phase wording
- Home quick links 补齐 `Collections`
- Home / Settings / Search / Onboarding 的 page copy 对齐 frozen v1 口径
- Onboarding / Home / Settings 的 source-related wording 更一致
- 允许范围内的 release-facing 文档已切换到 Windows-safe 文件名口径
- 当前状态档案补齐对应 current-state 文案说明

## 3. 当前 release-facing 检查口径

当前 release/polish 只保留以下 current-state 检查口径：

- 主页面与共享 source surface 的 wording 保持一致
- canonical release-facing 文档继续使用当前 Windows-safe 文件名
- frozen v1 与 v1.1 文档继续保持 current-state-only
- `README.md` 作为 concise operational entrypoint 保持可读、准确、不过度扩张

## 4. 当前明确不纳入本清单的事项

以下内容不属于 release/polish 小项，应继续留在 frozen v1 边界外：

- backend 行为调整
- retrieval 逻辑调整
- runtime 语义扩张
- 新页面 / 新 feature surface
- richer media / runtime / retrieval systems
